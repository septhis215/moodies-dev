FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Copy everything (but override with volume later)
COPY . .

EXPOSE 3000

# Run dev server with turbopack
CMD ["npm", "run", "dev"]
