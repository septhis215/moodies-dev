/**
 * Root Next.js loading segment — shown while any page in the app is loading.
 * The canonical loading UI lives in @/components/ui/AppLoading.
 *
 * To add the same screen to any other route segment, create a loading.tsx
 * next to that route's page.tsx with a single line:
 *   export { default } from "@/components/ui/AppLoading";
 */
export { default } from "@/components/ui/AppLoading";
